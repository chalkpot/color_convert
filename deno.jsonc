{
  "tasks": {
    "dev": "deno run --watch main.ts",
    "fmt": "deno fmt",
    "lint": "deno lint",
    "test": "deno test -A",
    "generate": "cd _generator && deno run -A _generator.ts && deno task fmt"
  }
}
